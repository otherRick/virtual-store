import { NavigationContainer } from '@react-navigation/native';
import { Navigator } from './src/navigation';

export default () => {
  return (
    <NavigationContainer>
      <Navigator />
    </NavigationContainer>
  );
};
