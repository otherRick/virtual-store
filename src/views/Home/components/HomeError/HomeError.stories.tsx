import { Meta } from '@storybook/react-native';
import { HomeError } from './HomeError';

const HomeErrorMata: Meta = {
  component: HomeError,
  title: 'View/Home',
};

export const HomeErrorDefault = () => {
  return <HomeError />;
};

export default HomeErrorMata;
