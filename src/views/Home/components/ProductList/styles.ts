import { StyleSheet } from 'react-native';

export default StyleSheet.create({
  container: {
    alignItems: 'center',
    justifyContent: 'center',
  },
  button: {
    width: '100%',
  },
  card: {
    borderBottomWidth: 1,
    flexDirection: 'row',
    padding: 20,
  },
  name: { flexDirection: 'row' },
  lenguage: { flexDirection: 'row' },
  area: {
    flexDirection: 'row',
    alignItems: 'center',
  },
});
